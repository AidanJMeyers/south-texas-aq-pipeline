# Docs-only dependencies for building the MkDocs site.
# Separate from requirements.txt so pipeline users don't need them.
#
# Install:  pip install -r requirements-docs.txt
# Preview:  mkdocs serve
# Build:    mkdocs build
# Deploy:   mkdocs gh-deploy        # (or push to main and let CI do it)

mkdocs>=1.5
mkdocs-material>=9.5
mkdocs-material-extensions>=1.3
pymdown-extensions>=10.0
mkdocs-git-revision-date-localized-plugin>=1.2
pygments>=2.17
